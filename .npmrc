engine-strict=true
legacy-peer-deps=true
# npm 12 blocks git deps; fabricjs-psbrush is a root git dependency.
# "all" and not "root": npm 11.9 to 11.12.1 refuse root deps too (npm/cli#9237,
# fixed in 11.13.0). Back to "root" once staging runs npm >= 11.13.
allow-git=all
